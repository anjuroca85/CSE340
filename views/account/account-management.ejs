<!-- account-management.ejs -->
<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
<% } %>

<%- messages() %>

<p><a href="/account/favorites">My Favorites</a></p>

<% if (locals.accountData) { %>

    <h2>Welcome <%= locals.accountData.account_firstname %>
    </h2>

    <p>
        <a href="/account/update/<%= locals.accountData.account_id %>">
            Update Account Information
        </a>
    </p>

<% if (locals.accountData.account_type==="Employee" || locals.accountData.account_type==="Admin" ) { %>
    <h3>Inventory Management</h3>
    <p>
        <a href="/inv/">Go to Inventory Management</a>
    </p>
<% } %>
<% } %>